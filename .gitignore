node_modules/
.next/
dist/
build/
out/
.turbo/
.env
.env.local
.env.*.local
*.log
npm-debug.log*

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Docker volumes
docker-data/
.env.prod

# Vertex service-account keys (mounted into api container, never committed)
secrets/

# Personal documents — resumes, trackers. Never commit these.
*.pdf
*.xlsx
Suyash_Tripathi_Resume.html

# Generated by a local `npm install` in services/renderer; the Dockerfile
# copies only package.json and runs `npm install`, so a lockfile here is unused.
services/renderer/package-lock.json

# Gate 0 backups — dumps live on D:\CareerOS-Backups and OneDrive, never in git
CareerOS-Backups/
*.dump

# Seed worklists DERIVED from a third-party directory are not committed.
# The extractor script is reproducible and belongs in the repo; the extracted
# selection of companies is substantially someone else's curation work, so it
# stays local. scripts/seeds/bangalore-pilot.tsv is our own hand-compiled list
# and IS tracked.
scripts/seeds/bangalore-full.tsv
scripts/seeds/bangalore-cohort250.tsv
